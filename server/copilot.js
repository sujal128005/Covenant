'use strict';
// Renamed to rationale.js. Kept only because the running dev server holds a
// file lock on Windows; safe to delete once the server is stopped.
module.exports = require('./rationale');
